---
import { cn } from "@src/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class={cn(
    "prose text-foreground prose-a:no-underline leading-normal",
    className
  )}
>
  <slot />
</div>

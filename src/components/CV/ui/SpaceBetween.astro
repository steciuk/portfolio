---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['flex items-baseline justify-between', className]}>
  <slot name='left' />
  <slot name='right' />
</div>

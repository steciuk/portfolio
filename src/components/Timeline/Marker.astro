---
interface Props {
  text: string;
  heightPx: number;
}

const { text, heightPx } = Astro.props;
---

<div
  class={`flex items-center gap-2`}
  style={{
    height: `${heightPx}px`,
  }}
>
  <div>{text}</div>
  <div class="inline-block h-0.5 w-8 bg-foreground"></div>
</div>
